export enum ChainFileName {
  ASSETLIST = "assetlist.json",
  CHAIN = "chain.json",
  VERSIONS = "versions.json"
}

export enum ChainDirName {
  IMAGES = "images"
}

export enum ChainTypeDirName {
  COSMOS = "",
  NON_COSMOS = "_non-cosmos"
}